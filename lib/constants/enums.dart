part of 'app_constants.dart';

enum AsyncState {
  loading,
  error,
  ready,
}

enum UserType {
  mentor,
  entrepreneur,
}

enum MessageDirection {
  sent,
  received,
  unset,
}

enum GroupIdent {
  mentors,
  mentees,
}

enum WebsiteLabels {
  facebook,
  twitter,
  venture,
  linkedin,
  other,
}
